public enum Team
{
    Green,
    Brown
}
public enum MovementType
{
    Wheels,
    Treads
}

public enum RangeType
{
    Tracking,
    MaxRange,
    MinRange
}

public enum VehicleType
{
    Jeep,
    Tank
}